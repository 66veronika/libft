stash_has_newline(stash)

extract_line(stash)
→ returns a malloc’d string ending with \n (unless EOF)

trim_stash(stash)
→ removes the returned line from stash and keeps the rest




get_next_line.c

get_next_line()

read_and_stash()

extract_line()

clean_stash()

get_next_line_utils.c

ft_strlen()

ft_strchr()

ft_strjoin_free() (joins and frees old stash)